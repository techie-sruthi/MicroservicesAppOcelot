docker logout
docker pull mongo:7.0
docker pull mcr.microsoft.com/mssql/server:2022-latest
docker pull quay.io/minio/minio:latest
docker-compose up -d

docker-compose up -d

docker ps

# Logout from Docker Hub
docker logout

# Then try again
docker-compose up -d


Complete Docker Deployment Guide

# 1. Build your image
docker build -t yourusername/productservice:latest .

# 2. Login to Docker Hub
docker login

# 3. Push to Docker Hub
docker push yourusername/productservice:latest

# Others can pull it:
docker pull yourusername/productservice:latest
// docker.io/library/microservicesapp-userservice:latest


docker-compose down
docker-compose up -d --build

docker logs apigateway


Option 1: SQL Server Management Studio (SSMS)
•	Server: localhost,1433
•	Auth: SQL Server Authentication
•	User: sa
•	Password: YourStrong@Passw0rd
Option 2: Terminal
docker exec -it sqlserver /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong@Passw0rd" -C -d UserServiceDb -Q "SELECT Id, UserName, Email, Role FROM Users;"

Option 1: MongoDB Compass 
•	Connect to: mongodb://localhost:27017
•	Database: ProductServiceDb
•	Collection: Products
Option 2: Terminal
docker exec -it mongodb mongosh --eval "db.getSiblingDB('ProductServiceDb').Products.find().pretty();"

•	Open browser: http://localhost:9001
•	User: minioadmin
•	Password: minioadmin
•	Bucket: product-images

docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

docker-compose build userservice