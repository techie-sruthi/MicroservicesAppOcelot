services:
  # MongoDB for ProductService
  mongodb:
    image: mongo:7.0
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ProductServiceDb
    volumes:
      - mongodb_data:/data/db
    networks:
      - microservices-network

  # SQL Server for UserService
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver
    ports:
      - "1433:1433"
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "YourStrong@Passw0rd"
      MSSQL_PID: "Express"
    volumes:
      - sqlserver_data:/var/opt/mssql
    networks:
      - microservices-network

  # MinIO for File Storage (S3-compatible)
  minio:
    image: quay.io/minio/minio:latest
    container_name: minio
    ports:
      - "9000:9000"      # API port
      - "9001:9001"      # Console UI port
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    networks:
      - microservices-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

      # not needed in development

  # UserService API
  userservice:
    build:
      context: ./Services/UserService
      dockerfile: UserService.API/Dockerfile
    container_name: userservice
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
      - USERSERVICE_PORT=5000
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=UserServiceDb;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True
      - Jwt__Key=THIS_IS_MY_SUPER_SECRET_KEY_12345
      - Jwt__Issuer=UserService
      - Jwt__Audience=MicroserviceUsers
      - Jwt__ExpiryMinutes=60
      - EmailSettings__SmtpServer=smtp.gmail.com
      - EmailSettings__SmtpPort=587
      - EmailSettings__SenderEmail=techie.sruthi23@gmail.com
      - EmailSettings__SenderName=Product Manager
      - EmailSettings__Password=orpm mdeu dzsr plns
      - EmailSettings__EnableSSL=true
    depends_on:
      - sqlserver
    networks:
      - microservices-network

  # ProductService API
  productservice:
    build:
      context: ./Services/ProductService
      dockerfile: ProductService.API/Dockerfile
    container_name: productservice
    ports:
      - "5050:5050"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5050
      - PRODUCTSERVICE_PORT=5050
      - MongoDB__ConnectionString=mongodb://mongodb:27017
      - MongoDB__DatabaseName=ProductServiceDb
      - FileStorage__Provider=MinIO
      - FileStorage__MinIO__Endpoint=minio:9000
      - FileStorage__MinIO__AccessKey=minioadmin
      - FileStorage__MinIO__SecretKey=minioadmin
      - FileStorage__MinIO__BucketName=product-images
      - FileStorage__MinIO__UseSSL=false
      - Jwt__Key=THIS_IS_MY_SUPER_SECRET_KEY_12345
    depends_on:
      - mongodb
      - minio
    networks:
      - microservices-network

  # API Gateway (Ocelot)
  apigateway:
    build:
      context: ./Services/ApiGateway/ApiGateway
      dockerfile: Dockerfile
    container_name: apigateway
    ports:
      - "5200:5200"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5200
      - OCELOT_CONFIG=ocelot.docker.json
    depends_on:
      - userservice
      - productservice
    networks:
      - microservices-network

  # Angular Frontend
  # angular-app:
  #   build:
  #     context: ./client
  #     dockerfile: Dockerfile
  #   container_name: angular-app
  #   ports:
  #     - "4200:80"
  #   depends_on:
  #     - userservice
  #     - productservice
  #   networks:
  #     - microservices-network

volumes:
  mongodb_data:
  sqlserver_data:
  minio_data:

networks:
  microservices-network:
    driver: bridge
